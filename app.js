let base=JSON.parse(JSON.stringify(window.CAMPAIGN_DATA||{})),clone=x=>JSON.parse(JSON.stringify(x)),uid=p=>p+"-"+Date.now().toString(36)+"-"+Math.random().toString(36).slice(2,5);
function newEncounter(name="Encounter"){return{id:uid("enc"),name,round:1,index:0,combatants:[],bystanders:[],effects:[],concentration:{}}}
function fresh(){let e=newEncounter("Encounter 1");return{characters:clone(base.characters||[]),creatures:clone(base.creatures||[]),library:clone(base.library||{spells:[],weapons:[],armor:[],items:[]}),notes:"",sessionName:"",encounters:[e],activeEncounterId:e.id,changes:[],undo:[]}}
let state=JSON.parse(localStorage.getItem("dnd-state")||"null")||fresh();state.characters??=clone(base.characters||[]);state.creatures??=clone(base.creatures||[]);state.bystanders??=clone(base.bystanders||[]);state.library??=clone(base.library||{});state.undo??=[];if(!state.encounters){let old=state.combat||newEncounter("Encounter 1");old.id=old.id||uid("enc");old.name=old.name||"Encounter 1";state.encounters=[old];state.activeEncounterId=old.id;delete state.combat}for(let k of ["spells","weapons","armor","items","actions"])state.library[k]??=[];const mergeSeed=(cur,seed)=>{for(const x of seed||[])if(!cur.some(y=>y.id===x.id))cur.push(clone(x))};mergeSeed(state.characters,base.characters);mergeSeed(state.creatures,base.creatures);mergeSeed(state.bystanders,base.bystanders);for(const k of ["spells","weapons","armor","items","actions"]){mergeSeed(state.library[k],base.library?.[k]);for(const seed of base.library?.[k]||[]){let live=state.library[k].find(x=>x.id===seed.id);if(live)for(const [key,val] of Object.entries(seed))if(live[key]===undefined)live[key]=clone(val)}}for(const x of state.characters){const s=base.characters?.find(y=>y.id===x.id);if(s?.knownActions){x.knownActions??=[];for(const a of s.knownActions)if(!x.knownActions.includes(a))x.knownActions.push(a)}if(s?.spellcastingAbility&&!x.spellcastingAbility)x.spellcastingAbility=s.spellcastingAbility}for(const x of state.creatures){const s=base.creatures?.find(y=>y.id===x.id);if(s?.knownActions){x.knownActions??=[];for(const a of s.knownActions)if(!x.knownActions.includes(a))x.knownActions.push(a)}}for(let e of state.encounters){e.bystanders??=[];e.combatants??=[];for(let c of e.combatants){c.role??="ally";c.instance??=null}}for(let c of state.characters){let seed=base.characters?.find(x=>x.id===c.id);for(let k of ["abilities","saveProficiencies","skills","inventory","equipment","knownActions"])if(c[k]==null)c[k]=clone(seed?.[k]??(k==="abilities"?{str:10,dex:10,con:10,int:10,wis:10,cha:10}:k==="equipment"?{}:k==="skills"?{}:[]));c.proficiency??=seed?.proficiency??2;c.tempHp??=0}
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];let selectedChar=null,selectedCreature=null,libType="spells",selectedLib=null,modalState=null;function enc(){return state.encounters.find(x=>x.id===state.activeEncounterId)||state.encounters[0]}function allEntities(){return[...state.characters,...state.creatures,...state.bystanders]}function entity(id){return allEntities().find(x=>x.id===id)}function sourceEntity(type,id){if(type==="character")return state.characters.find(x=>x.id===id);if(type==="creature")return state.creatures.find(x=>x.id===id);if(type==="bystander")return state.bystanders.find(x=>x.id===id);return entity(id)}function icon(c){return c?.icon||({"Sorcerer":"🧙","Wizard":"🧙","Cleric":"⛑️","Druid":"🌿","Fighter":"🪖","Rogue":"🥷"}[c?.class]||"👤")}function combatEntity(cmb){let t=sourceEntity(cmb.sourceType,cmb.entityId);if(!t)return null;return cmb.instance?{...t,...cmb.instance,id:cmb.id,templateId:t.id}:{...t,id:cmb.id,templateId:t.id}}function targets(){return[...enc().combatants,...enc().bystanders].map(x=>({cmb:x,c:combatEntity(x)})).filter(x=>x.c)}function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]))}function mod(n){return Math.floor((+n-10)/2)}function fmtMod(n){return n>=0?"+"+n:String(n)}function profLevel(v,p){return v==="expertise"?p*2:v==="proficient"?p:0}function saveBonus(c,k){return mod(c.abilities?.[k]||10)+(c.saveProficiencies||[]).includes(k)*(c.proficiency||0)}function skillAbility(k){return({athletics:"str",acrobatics:"dex",sleightOfHand:"dex",stealth:"dex",arcana:"int",history:"int",investigation:"int",nature:"int",religion:"int",animalHandling:"wis",insight:"wis",medicine:"wis",perception:"wis",survival:"wis",deception:"cha",intimidation:"cha",performance:"cha",persuasion:"cha"})[k]||"wis"}function skillBonus(c,k){return mod(c.abilities?.[skillAbility(k)]||10)+profLevel(c.skills?.[k],c.proficiency||0)}function parsePool(v){let[a,b]=String(v??"0/0").split("/").map(Number);return{cur:a||0,max:Number.isFinite(b)?b:(a||0)}}function setPool(o,k,n){let p=parsePool(o[k]);o[k]=Math.max(0,n)+"/"+p.max}function effectsFor(id){return enc().effects.filter(e=>e.targetId===id)}function itemBy(id){for(let t of ["spells","weapons","armor","items","actions"]){let x=state.library[t].find(i=>i.id===id);if(x)return{...x,libType:t}}}function effectiveAC(c){let ac=+c.ac||10,arm=itemBy(c.equipment?.armor),off=itemBy(c.equipment?.offHand);if(arm?.ac)ac=+arm.ac;if(off?.acBonus)ac+=+off.acBonus;return ac+effectsFor(c.id).reduce((n,e)=>n+(+e.ac||0),0)}function snap(){return JSON.stringify({characters:state.characters,creatures:state.creatures,bystanders:state.bystanders,encounters:state.encounters,library:state.library})}function save(kind,detail,before){if(kind){state.changes.push({at:new Date().toISOString(),kind,detail});if(before){state.undo.push({before,label:typeof detail==="string"?detail:(detail?.action||kind)});if(state.undo.length>30)state.undo.shift()}}localStorage.setItem("dnd-state",JSON.stringify(state));render()}function actionsFor(c){let ids=new Set(c.knownActions||[]);for(let x of c.inventory||[])if(x.category==="weapons")ids.add(x.ref);return [...ids].map(itemBy).filter(Boolean)}function economy(cmb){cmb.economy??={action:false,bonus:false,reaction:false,movement:0,extraActions:0};return cmb.economy}function actionType(a){return a.actionType||(/Bonus/i.test(a.kind||"")?"bonus":/Reaction/i.test(a.kind||"")?"reaction":"action")}function d(n){return 1+Math.floor(Math.random()*n)}function rollFormula(f){let s=String(f||"0").replace(/\s+/g,""),m=s.match(/^(\d*)d(\d+)([+-]\d+)?$/i);if(!m)return{total:+s||0,rolls:[]};let count=+(m[1]||1),sides=+m[2],flat=+(m[3]||0),rolls=[];for(let i=0;i<count;i++)rolls.push(d(sides));return{total:rolls.reduce((a,b)=>a+b,0)+flat,rolls}}function spellAbility(c){return c.spellcastingAbility||({Cleric:"wis",Druid:"wis",Sorcerer:"cha",Wizard:"int",Bard:"cha"}[c.class]||"wis")}function attackAbility(c,a){let k=a.attackAbility;if(k==="spell")k=spellAbility(c);if(k==="finesse")return Math.max(mod(c.abilities?.str||10),mod(c.abilities?.dex||10));return mod(c.abilities?.[k||"str"]||10)}function attackBonus(c,a){return a.attackBonus!=null?+a.attackBonus:attackAbility(c,a)+(a.proficient===false?0:(c.proficiency||0))}function spellDC(c,a){return a.saveDC||8+(c.proficiency||0)+mod(c.abilities?.[spellAbility(c)]||10)}function combatantById(id){return[...enc().combatants,...enc().bystanders].find(x=>x.id===id)}function liveCombatant(cmb){return cmb.instance||sourceEntity(cmb.sourceType,cmb.entityId)}function applyDamage(cmb,n){let t=liveCombatant(cmb),left=Math.max(0,n);t.tempHp??=0;if(t.tempHp){let used=Math.min(t.tempHp,left);t.tempHp-=used;left-=used}t.hp=Math.max(0,t.hp-left);return n}function applyHeal(cmb,n){let t=liveCombatant(cmb),before=t.hp;t.hp=Math.min(t.maxHp,t.hp+n);return t.hp-before}function effectRoll(id,key){let total=0;for(const fx of effectsFor(id))if(fx[key])total+=rollFormula(fx[key]).total;return total}function applyActionEffects(a,casterId,targetIds,list){for(const fx of(list||a.effects||[]))for(const tid of(fx.target==="self"?[casterId]:targetIds)){enc().effects.push({...fx,id:uid("fx"),targetId:tid,sourceId:casterId,sourceActionId:a.id});if(fx.condition){let t=combatantById(tid),live=t&&liveCombatant(t);if(live){live.conditions??=[];if(!live.conditions.includes(fx.condition))live.conditions.push(fx.condition)}}}}function combatLog(text){enc().log??=[];enc().log.unshift({at:new Date().toISOString(),text});if(enc().log.length>30)enc().log.length=30}function openModal(title,body,footer="",eyebrow="SELECT"){modalState={};$("#modalTitle").textContent=title;$("#modalEyebrow").textContent=eyebrow;$("#modalBody").innerHTML=body;$("#modalFooter").innerHTML=footer;$("#modal").hidden=false}function closeModal(){$("#modal").hidden=true;modalState=null}function choiceCard(c,attrs=""){return '<button class="choice" '+attrs+'><span class="role-icon">'+icon(c)+'</span><span><strong>'+esc(c.name)+'</strong><small>'+esc(c.class||c.creatureType||c.type||"")+'</small></span></button>'}function addParticipantModal(){openModal("Add participant",'<div class="role-picker"><button data-pick-role="ally" class="selected">Ally</button><button data-pick-role="enemy">Enemy</button><button data-pick-role="bystander">Bystander</button></div><input id="participantSearch" placeholder="Search characters, creatures or bystanders…"><p class="meta">Choose a role, then select from Characters, Creatures & Monsters, or Bystander Types.</p><div id="participantChoices"></div>','','ENCOUNTER');modalState={role:"ally"};renderParticipantChoices("")}function renderParticipantChoices(q){q=(q||"").toLowerCase();let section=(title,type,arr)=>{let found=arr.filter(x=>(x.name+" "+(x.class||x.creatureType||"")).toLowerCase().includes(q));return '<div class="picker-section"><h3>'+title+'</h3>'+(found.length?'<div class="choice-grid">'+found.map(x=>choiceCard(x,'data-pick-participant="'+x.id+'" data-source-type="'+type+'"')).join("")+'</div>':'<p class="meta">No matches.</p>')+'</div>'};$("#participantChoices").innerHTML=section("Characters","character",state.characters)+section("Creatures & Monsters","creature",state.creatures)+section("Bystander Types","bystander",state.bystanders)}function slotModal(c,a,av){openModal("Choose spell slot",'<div class="choice-grid">'+av.map(k=>'<button class="choice" data-pick-slot="'+k+'"><span><strong>Level '+k+'</strong><small>'+esc(c.spellSlots[k])+' remaining</small></span></button>').join("")+'</div>','','CAST '+a.name)}function targetModal(c,a,slot){let mode=a.target?.mode||((a.effects||[]).some(e=>e.target==="selected")?"single":"self"),max=a.target?.max||1;if(mode==="self")return finishAction(c,a,slot,[c.id]);let body='<p class="meta">'+(mode==="multi"?"Choose up to "+max+" targets.":"Choose one target.")+'</p><div class="target-grid">'+targets().map(({cmb,c:x})=>'<button class="target-choice" data-pick-target="'+cmb.id+'"><span class="role-icon">'+icon(x)+'</span><span><strong>'+esc(cmb.name)+'</strong><small>'+esc(cmb.role||"bystander")+' · '+x.hp+'/'+x.maxHp+' HP</small></span></button>').join("")+'</div>';openModal("Choose target"+(mode==="multi"?"s":""),body,mode==="multi"?'<button id="confirmTargets" class="primary" disabled>Apply</button>':"","TARGET");modalState={caster:c.id,action:a.id,slot,mode,max,selected:[]}}function finiteChoice(title,items,onAttr){openModal(title,'<div class="choice-grid">'+items.map(x=>choiceCard(x,onAttr+'="'+x.id+'"')).join("")+'</div>')}function numericModal(title,label,quick,cb){openModal(title,'<label>'+esc(label)+'<input id="numericValue" type="number" inputmode="numeric" value="'+(quick[0]||0)+'"></label><div class="quick-number">'+quick.map(n=>'<button data-quick-number="'+n+'">'+n+'</button>').join("")+'</div>','<button id="numericConfirm" class="primary">Apply</button>');modalState={numericCallback:cb}}
function render(){let e=enc(),pcs=state.characters.filter(c=>c.type==="PC");$("#stats").innerHTML=[["Party",pcs.length],["Creatures",state.creatures.length],["Encounters",state.encounters.length],["Changes",state.changes.length]].map(x=>'<div class="stat"><small>'+x[0]+'</small><strong>'+x[1]+'</strong></div>').join("");$("#partyCards").innerHTML=pcs.map(c=>'<div class="card" data-char="'+c.id+'"><div class="portrait">'+esc(c.name[0])+'</div><strong>'+esc(c.name)+'</strong><div class="meta">'+esc(c.class)+' · Level '+c.level+'</div><div class="hpbar"><i style="width:'+Math.max(0,Math.min(100,c.hp/c.maxHp*100))+'%"></i></div><div class="meta">'+c.hp+'/'+c.maxHp+' HP · AC '+effectiveAC(c)+'</div></div>').join("");renderCharacters();renderCreatures();renderLibrary();renderCombat();$("#sessionNotes").value=state.notes;$("#sessionName").value=state.sessionName;$("#sessionTitle").textContent=state.sessionName||"Untitled Session"}function renderCharacters(){let q=($("#search")?.value||"").toLowerCase();$("#characterList").innerHTML=state.characters.filter(c=>(c.name+" "+c.class).toLowerCase().includes(q)).map(c=>'<div class="list-item" data-char="'+c.id+'"><span><strong>'+esc(c.name)+'</strong><br><small>'+esc(c.class)+' '+c.level+'</small></span><span>AC '+effectiveAC(c)+'</span></div>').join("");if(selectedChar)detail(selectedChar,"#characterDetail",true)}function abilityHTML(c,editable){return '<div class="ability-grid">'+["str","dex","con","int","wis","cha"].map(k=>'<div class="ability"><small>'+k.toUpperCase()+'</small>'+(editable?'<input data-ability="'+c.id+'" data-ability-key="'+k+'" type="number" value="'+c.abilities[k]+'">':'<strong>'+c.abilities[k]+'</strong>')+'<span>'+fmtMod(mod(c.abilities[k]))+'</span></div>').join("")+'</div>'}function detail(id,target,editable=false){let c=entity(id)||targets().map(x=>x.c).find(x=>x.id===id);if(!c)return;let fx=effectsFor(id),actions=actionsFor(c),saves=["str","dex","con","int","wis","cha"],skills=Object.keys(c.skills||{});$(target).classList.remove("empty");$(target).innerHTML='<div class="detail-head"><div class="avatar">'+esc(c.name[0])+'</div><div><small>'+esc(c.type||"PC")+'</small><h2>'+esc(c.name)+'</h2><div class="meta">'+esc(c.class||c.creatureType||"")+(c.level?" · Level "+c.level:"")+'</div></div></div>'+abilityHTML(c,editable)+'<div class="resource-grid"><div class="resource"><small>HP</small><strong>'+c.hp+'/'+c.maxHp+(c.tempHp?" +"+c.tempHp+" temp":"")+'</strong></div><div class="resource"><small>AC</small><strong>'+effectiveAC(c)+'</strong></div><div class="resource"><small>PROF</small><strong>'+fmtMod(c.proficiency||0)+'</strong></div><div class="resource"><small>SPEED</small><strong>'+c.speed+' ft</strong></div></div><div class="sheet-grid"><div class="sheet-section"><h3>Saving throws</h3>'+saves.map(k=>'<div class="kv"><span>'+k.toUpperCase()+((c.saveProficiencies||[]).includes(k)?" ●":"")+'</span><strong>'+fmtMod(saveBonus(c,k))+'</strong></div>').join("")+'</div><div class="sheet-section"><h3>Skills</h3>'+(skills.length?skills.map(k=>'<div class="kv"><span>'+esc(k)+' '+(c.skills[k]==="expertise"?"◆":c.skills[k]==="proficient"?"●":"")+'</span><strong>'+fmtMod(skillBonus(c,k))+'</strong></div>').join(""):'<span class="meta">No skills entered.</span>')+'</div></div>'+(editable?editPanel(c):"")+(fx.length?'<h3 style="margin-top:16px">Active effects</h3><div class="effect-list">'+fx.map(e=>'<div class="effect"><span><strong>'+esc(e.name)+'</strong><small>'+esc(e.note||"")+' · '+esc(e.duration)+'</small></span><button data-remove-effect="'+e.id+'">×</button></div>').join("")+'</div>':'')+'<h3 style="margin-top:18px">Moves & spells</h3><div class="action-list">'+actions.map(a=>'<button class="action-card" data-use-action="'+c.id+'" data-action="'+a.id+'"><span><strong>'+esc(a.name)+'</strong><small>'+esc(a.category)+' · '+esc(actionType(a))+'</small></span><span class="meta">'+esc(a.summary||"")+'</span></button>').join("")+'</div>'}function editPanel(c){let weapons=state.library.weapons,armor=state.library.armor;let opts=(arr,val)=>'<option value="">— none —</option>'+arr.map(x=>'<option value="'+x.id+'" '+(x.id===val?"selected":"")+'>'+esc(x.name)+'</option>').join("");return '<h3 style="margin-top:18px">Editable sheet</h3><div class="editor-row"><label>HP<input data-edit="'+c.id+'" data-key="hp" type="number" value="'+c.hp+'"></label><label>Max HP<input data-edit="'+c.id+'" data-key="maxHp" type="number" value="'+c.maxHp+'"></label><label>Temp HP<input data-edit="'+c.id+'" data-key="tempHp" type="number" value="'+c.tempHp+'"></label><label>Base AC<input data-edit="'+c.id+'" data-key="ac" type="number" value="'+c.ac+'"></label><label>Speed<input data-edit="'+c.id+'" data-key="speed" type="number" value="'+c.speed+'"></label><label>Proficiency<input data-edit="'+c.id+'" data-key="proficiency" type="number" value="'+c.proficiency+'"></label></div><h3 style="margin-top:15px">Equipment</h3><div class="equip-grid"><label>Main hand<select data-equip="'+c.id+'" data-slot="mainHand">'+opts(weapons,c.equipment?.mainHand)+'</select></label><label>Off hand<select data-equip="'+c.id+'" data-slot="offHand">'+opts([...weapons,...armor.filter(x=>x.slot==="offhand")],c.equipment?.offHand)+'</select></label><label>Armour<select data-equip="'+c.id+'" data-slot="armor">'+opts(armor.filter(x=>x.slot!=="offhand"),c.equipment?.armor)+'</select></label></div><div class="mini-actions" style="margin-top:10px"><button data-add-inventory="'+c.id+'">Give from library</button><button data-add-action="'+c.id+'">Learn spell/action</button><button data-edit-saves="'+c.id+'">Edit saves</button><button data-edit-skills="'+c.id+'">Edit skills</button></div>'}function renderCreatures(){$("#creatureList").innerHTML=state.creatures.map(c=>'<div class="list-item" data-creature="'+c.id+'"><span><strong>'+esc(c.name)+'</strong><br><small>'+esc(c.size||"")+' '+esc(c.creatureType||"Creature")+'</small></span><span>AC '+effectiveAC(c)+'</span></div>').join("");if(selectedCreature)detail(selectedCreature,"#creatureDetail",true)}function renderLibrary(){$$(".library-tabs button").forEach(b=>b.classList.toggle("active",b.dataset.lib===libType));$("#libraryList").innerHTML=state.library[libType].map(x=>'<div class="list-item" data-library-id="'+x.id+'"><span><strong>'+esc(x.name)+'</strong><br><small>'+esc(x.category||libType)+'</small></span><span>'+esc(x.level!=null?"Lv "+x.level:x.damage||x.ac||"")+'</span></div>').join("");let x=state.library[libType].find(y=>y.id===selectedLib);if(x){$("#libraryDetail").classList.remove("empty");$("#libraryDetail").innerHTML='<div class="library-form"><small>'+esc(libType.toUpperCase())+'</small><h2>'+esc(x.name)+'</h2><label>Name<input data-lib-edit="'+x.id+'" data-field="name" value="'+esc(x.name)+'"></label><label>Summary<textarea data-lib-edit="'+x.id+'" data-field="summary">'+esc(x.summary||"")+'</textarea></label><div class="meta">ID: '+esc(x.id)+' · All reusable records are editable here; advanced mechanics remain represented by their structured fields.</div></div>'}}function renderCombat(){let e=enc();$("#encounterSelect").innerHTML=state.encounters.map(x=>'<option value="'+x.id+'" '+(x.id===e.id?"selected":"")+'>'+esc(x.name)+'</option>').join("");$("#round").textContent=e.round;let groups={ally:e.combatants.filter(x=>x.role==="ally"),enemy:e.combatants.filter(x=>x.role==="enemy"),bystander:e.bystanders};$("#combatRoster").innerHTML=Object.entries(groups).map(([role,list])=>'<div class="roster-group"><h3>'+({ally:"Allies",enemy:"Enemies",bystander:"Bystanders"})[role]+'</h3>'+(list.length?list.map(cmb=>{let c=combatEntity(cmb);return '<div class="roster-entry '+role+'-mark '+((c.hp||0)<=0?"dead":"")+'"><span class="role-icon">'+icon(c)+'</span><span class="who"><strong>'+esc(cmb.name)+'</strong><small>'+c.hp+'/'+c.maxHp+' HP · AC '+effectiveAC(c)+'</small></span><button data-roster-damage="'+cmb.id+'">HP</button></div>'}).join(""):'<span class="meta">None</span>')+'</div>').join("");$("#initiative").innerHTML=e.combatants.length?e.combatants.map((x,i)=>{let live=combatEntity(x);return '<div class="init-row '+(i===e.index?"active":"")+'" data-turn="'+i+'"><span class="init-main"><span class="role-icon">'+icon(live)+'</span><span><strong>'+esc(x.name)+'</strong><br><small>'+live.hp+'/'+live.maxHp+' HP · '+esc(x.role)+'</small></span></span><strong class="init-score">'+x.roll+'</strong></div>'}).join(""):'<div class="meta">No initiative participants yet.</div>';if(e.combatants.length){let cmb=e.combatants[e.index],c=combatEntity(cmb),eco=economy(cmb);detail(c.id,"#turnPanel",false);$("#turnPanel").insertAdjacentHTML("afterbegin",'<div class="encounter-name"><span class="role-icon big">'+icon(c)+'</span> '+esc(e.name)+'</div><div class="economy"><div class="econ '+(eco.action?"used":"")+'">Action '+(eco.action?"USED":"READY")+'</div><div class="econ '+(eco.bonus?"used":"")+'">Bonus '+(eco.bonus?"USED":"READY")+'</div><div class="econ '+(eco.reaction?"used":"")+'">Reaction '+(eco.reaction?"USED":"READY")+'</div><div class="econ">Move '+Math.max(0,(c.speed||0)-eco.movement)+' ft</div></div>');$("#turnPanel").insertAdjacentHTML("beforeend",'<div class="combat-footer"><button data-move="'+cmb.id+'">Use movement</button><button id="undoAction" '+(state.undo.length?"":"disabled")+'>Undo</button></div>')}else{$("#turnPanel").className="panel empty";$("#turnPanel").textContent="Add allies or enemies to begin initiative. Bystanders remain targetable in the roster."}}function useAction(id,aid){let c=entity(id)||targets().map(x=>x.c).find(x=>x.id===id),a=itemBy(aid);if(!c||!a)return;let e=enc(),cmb=e.combatants[e.index],eco=cmb&&combatEntity(cmb)?.id===id?economy(cmb):null,type=actionType(a);if(eco&&type==="action"&&eco.action&&eco.extraActions<1)return alert("Action already used.");if(eco&&type==="bonus"&&eco.bonus)return alert("Bonus action already used.");if(eco&&type==="reaction"&&eco.reaction)return alert("Reaction already used.");let slot=a.cost?.spellSlot;if(slot&&a.cost.selectSlot){let av=Object.keys(c.spellSlots||{}).filter(k=>+k>=slot&&parsePool(c.spellSlots[k]).cur>0);if(!av.length)return alert("No suitable spell slots.");modalState={caster:id,action:aid};return slotModal(c,a,av)}if(slot&&parsePool(c.spellSlots?.[slot]).cur<1)return alert("No slot remaining.");targetModal(c,a,slot)}function finishAction(c,a,slot,targetIds){let casterCmb=combatantById(c.id),before=snap(),liveCaster=casterCmb?liveCombatant(casterCmb):c,e=enc(),eco=casterCmb?economy(casterCmb):null,type=actionType(a);if(slot&&liveCaster.spellSlots)setPool(liveCaster.spellSlots,slot,parsePool(liveCaster.spellSlots[slot]).cur-1);if(eco){if(type==="action"){if(eco.action&&eco.extraActions>0)eco.extraActions--;else eco.action=true}else if(type==="bonus")eco.bonus=true;else if(type==="reaction")eco.reaction=true}if(a.concentration&&casterCmb){let old=e.concentration[casterCmb.id];if(old)e.effects=e.effects.filter(x=>x.sourceId!==casterCmb.id);e.concentration[casterCmb.id]=a.id}let results=[],resolution=a.resolution||"effect",casterId=casterCmb?.id||c.id;if(resolution==="attack"){let t=combatantById(targetIds[0]),target=combatEntity(t),raw=d(20),bonus=attackBonus(c,a)+effectRoll(casterId,"attackBonus"),total=raw+bonus,crit=raw===20,hit=crit||(raw!==1&&total>=effectiveAC(target)),damage=0;if(hit&&a.damage){damage=rollFormula(a.damage).total+(a.damageBonus||0)+(a.addAbilityToDamage?attackAbility(c,a):0);if(crit)damage+=rollFormula(a.damage).total;applyDamage(t,Math.max(0,damage))}if(hit&&a.effectsOnHit)applyActionEffects(a,casterId,[t.id],a.effectsOnHit);results.push((hit?"HIT":"MISS")+" — d20 "+raw+" "+fmtMod(bonus)+" = "+total+" vs AC "+effectiveAC(target)+(hit&&a.damage?" · "+damage+" "+(a.damageType||"")+" damage":""));combatLog(c.name+" used "+a.name+" on "+t.name+": "+results[0])}else if(resolution==="save"){let dc=spellDC(c,a),failed=[];for(const tid of targetIds){let t=combatantById(tid),target=combatEntity(t),raw=d(20),bonus=saveBonus(target,a.saveAbility||"dex")+effectRoll(tid,"saveBonus"),total=raw+bonus,ok=total>=dc,damage=0;if(a.damage&&(!ok||a.halfOnSave)){damage=rollFormula(a.damage).total;if(ok)damage=Math.floor(damage/2);if(damage)applyDamage(t,damage)}if(!ok)failed.push(tid);results.push(t.name+": "+(ok?"SAVE":"FAIL")+" — "+total+" vs DC "+dc+(damage?" · "+damage+" "+(a.damageType||"")+" damage":""))}if(failed.length)applyActionEffects(a,casterId,failed);combatLog(c.name+" used "+a.name+": "+results.join(" | "))}else if(resolution==="heal"){for(const tid of targetIds){let t=combatantById(tid),amt=rollFormula(a.heal||"1d4").total;if(a.healAbility==="spell")amt+=mod(c.abilities?.[spellAbility(c)]||10);if(slot&&a.upcastHeal&&slot>(a.cost?.spellSlot||1))for(let i=(a.cost?.spellSlot||1);i<slot;i++)amt+=rollFormula(a.upcastHeal).total;let actual=applyHeal(t,Math.max(0,amt));results.push(t.name+" healed "+actual+" HP")}combatLog(c.name+" used "+a.name+": "+results.join(" | "))}else{applyActionEffects(a,casterId,targetIds);results.push("Effect applied to "+targetIds.map(id=>combatantById(id)?.name).filter(Boolean).join(", "));combatLog(c.name+" used "+a.name+": "+results[0])}save("combat.action",{actor:c.name,action:a.name,slot:slot||null,targets:targetIds,results},before);openModal(a.name,results.map(x=>'<div class="resolution-result">'+esc(x)+'</div>').join(""),'<button id="modalDone" class="primary">Done</button>',resolution.toUpperCase())}function startTurn(cmb){let eco=economy(cmb);eco.action=false;eco.bonus=false;eco.movement=0;eco.extraActions=0;eco.reaction=false;enc().effects=enc().effects.filter(x=>!(x.targetId===cmb.id&&x.duration==="start-next-turn"))}document.addEventListener("click",e=>{if(e.target.id==="modalClose"||e.target.id==="modalDone"||e.target=== $("#modal"))return closeModal();let role=e.target.closest("[data-pick-role]");if(role){$("[data-pick-role]").forEach(x=>x.classList.remove("selected"));role.classList.add("selected");modalState={...(modalState||{}),role:role.dataset.pickRole};return}let pp=e.target.closest("[data-pick-participant]");if(pp){let type=pp.dataset.sourceType,x=sourceEntity(type,pp.dataset.pickParticipant),role=modalState?.role||"ally",before=snap(),used=[...enc().combatants,...enc().bystanders].filter(z=>z.entityId===x.id).length,name=used?x.name+" "+(used+1):x.name,cmb={id:uid("cmb"),sourceType:type,entityId:x.id,name,role,roll:+x.initiative||0,instance:type==="character"?null:clone(x),economy:{action:false,bonus:false,reaction:false,movement:0,extraActions:0}};if(role==="bystander"){enc().bystanders.push(cmb);closeModal();save("combat.bystander.add",name,before)}else{closeModal();numericModal("Initiative: "+name,"Initiative roll",[cmb.roll,10,15,20],n=>{cmb.roll=n;enc().combatants.push(cmb);enc().combatants.sort((a,b)=>b.roll-a.roll);closeModal();save("combat.add",{name,role},before)})}return}let ps=e.target.closest("[data-pick-slot]");if(ps){let c=entity(modalState.caster)||targets().map(x=>x.c).find(x=>x.id===modalState.caster),a=itemBy(modalState.action);return targetModal(c,a,+ps.dataset.pickSlot)}let pt=e.target.closest("[data-pick-target]");if(pt){if(modalState.mode==="single"){let c=entity(modalState.caster)||targets().map(x=>x.c).find(x=>x.id===modalState.caster),a=itemBy(modalState.action);return finishAction(c,a,modalState.slot,[pt.dataset.pickTarget])}let id=pt.dataset.pickTarget,i=modalState.selected.indexOf(id);if(i>=0)modalState.selected.splice(i,1);else if(modalState.selected.length<modalState.max)modalState.selected.push(id);pt.classList.toggle("selected",modalState.selected.includes(id));$("#confirmTargets").disabled=!modalState.selected.length;return}if(e.target.id==="confirmTargets"){let c=entity(modalState.caster)||targets().map(x=>x.c).find(x=>x.id===modalState.caster),a=itemBy(modalState.action);return finishAction(c,a,modalState.slot,modalState.selected)}let qn=e.target.closest("[data-quick-number]");if(qn){$("#numericValue").value=qn.dataset.quickNumber;return}if(e.target.id==="numericConfirm"&&modalState?.numericCallback){let cb=modalState.numericCallback,n=+$("#numericValue").value;return cb(n)}let tab=e.target.closest("[data-view]");if(tab){$$("[data-view]").forEach(x=>x.classList.remove("active"));tab.classList.add("active");$$(".view").forEach(x=>x.classList.toggle("active",x.id===tab.dataset.view))}let c=e.target.closest("[data-char]");if(c){selectedChar=c.dataset.char;$$("[data-view]").forEach(x=>x.classList.toggle("active",x.dataset.view==="characters"));$$(".view").forEach(x=>x.classList.toggle("active",x.id==="characters"));renderCharacters()}let cr=e.target.closest("[data-creature]");if(cr){selectedCreature=cr.dataset.creature;renderCreatures()}let li=e.target.closest("[data-library-id]");if(li){selectedLib=li.dataset.libraryId;renderLibrary()}let lt=e.target.closest("[data-lib]");if(lt){libType=lt.dataset.lib;selectedLib=null;renderLibrary()}let tr=e.target.closest("[data-turn]");if(tr){enc().index=+tr.dataset.turn;save()}let ua=e.target.closest("[data-use-action]");if(ua)useAction(ua.dataset.useAction,ua.dataset.action);if(e.target.id==="nextTurn"&&enc().combatants.length){let before=snap(),z=enc();z.index++;if(z.index>=z.combatants.length){z.index=0;z.round++}startTurn(z.combatants[z.index]);save("combat.turn","Advanced turn",before)}if(e.target.id==="newEncounterBtn"){let n=prompt("Encounter name","Encounter "+(state.encounters.length+1));if(n){let x=newEncounter(n);state.encounters.push(x);state.activeEncounterId=x.id;save("combat.new",n)}}if(e.target.id==="addCombatant")addParticipantModal();if(e.target.id==="undoAction"&&state.undo.length){let x=state.undo.pop(),r=JSON.parse(x.before);Object.assign(state,r);state.changes.push({at:new Date().toISOString(),kind:"undo",detail:x.label});save()}if(e.target.dataset.move){let cmb=enc().combatants.find(x=>x.id===e.target.dataset.move),c=combatEntity(cmb);numericModal("Movement: "+cmb.name,"Feet moved",[5,10,15,30],n=>{if(n>0){let before=snap();economy(cmb).movement=Math.min(c.speed,economy(cmb).movement+n);closeModal();save("combat.move",{actor:cmb.name,feet:n},before)}})}let rh=e.target.closest("[data-roster-damage]");if(rh){let cmb=[...enc().combatants,...enc().bystanders].find(x=>x.id===rh.dataset.rosterDamage),c=combatEntity(cmb);openModal(cmb.name,'<div class="resource-grid"><div class="resource"><small>HP</small><strong>'+c.hp+' / '+c.maxHp+'</strong></div><div class="resource"><small>AC</small><strong>'+effectiveAC(c)+'</strong></div></div><div class="hp-actions"><button data-hp-mode="damage" class="danger">Damage</button><button data-hp-mode="heal" class="success">Heal</button></div>');modalState={rosterId:cmb.id};return}let hm=e.target.closest("[data-hp-mode]");if(hm){let rid=modalState.rosterId,cmb=[...enc().combatants,...enc().bystanders].find(x=>x.id===rid),mode=hm.dataset.hpMode;numericModal((mode==="damage"?"Damage: ":"Heal: ")+cmb.name,"Amount",[1,5,10,20],n=>{let before=snap(),target=cmb.instance||entity(cmb.entityId);target.hp=mode==="damage"?Math.max(0,target.hp-n):Math.min(target.maxHp,target.hp+n);closeModal();save("combat."+mode,{target:cmb.name,amount:n},before)});return}if(e.target.dataset.addInventory){let c=entity(e.target.dataset.addInventory),all=["weapons","armor","items"].flatMap(t=>state.library[t].map(x=>({...x,libType:t})));openModal("Give item to "+c.name,'<div class="choice-grid">'+all.map(x=>'<button class="choice" data-give-item="'+x.id+'" data-give-type="'+x.libType+'" data-give-char="'+c.id+'"><span><strong>'+esc(x.name)+'</strong><small>'+esc(x.libType)+'</small></span></button>').join("")+'</div>')}let gi=e.target.closest("[data-give-item]");if(gi){let c=entity(gi.dataset.giveChar),x=itemBy(gi.dataset.giveItem);c.inventory.push({ref:x.id,category:gi.dataset.giveType,qty:1});closeModal();save("inventory.add",{character:c.name,item:x.name})}if(e.target.dataset.addAction){let c=entity(e.target.dataset.addAction),all=[...state.library.spells,...state.library.weapons];openModal("Add action to "+c.name,'<div class="choice-grid">'+all.map(x=>'<button class="choice" data-learn-action="'+x.id+'" data-learn-char="'+c.id+'"><span><strong>'+esc(x.name)+'</strong><small>'+esc(x.category)+'</small></span></button>').join("")+'</div>')}let la=e.target.closest("[data-learn-action]");if(la){let c=entity(la.dataset.learnChar),x=itemBy(la.dataset.learnAction);if(!c.knownActions.includes(x.id))c.knownActions.push(x.id);closeModal();save("action.learn",{character:c.name,action:x.name})}if(e.target.dataset.editSaves){let c=entity(e.target.dataset.editSaves),raw=prompt("Saving throw proficiencies (str,dex,con,int,wis,cha)",(c.saveProficiencies||[]).join(","));if(raw!=null){c.saveProficiencies=raw.toLowerCase().split(",").map(x=>x.trim()).filter(x=>["str","dex","con","int","wis","cha"].includes(x));save("character.saves",c.name)}}if(e.target.dataset.editSkills){let c=entity(e.target.dataset.editSkills),raw=prompt("Skill entries: skill=proficient, skill=expertise\nExample: perception=proficient,stealth=expertise",Object.entries(c.skills||{}).map(([k,v])=>k+"="+v).join(","));if(raw!=null){c.skills={};raw.split(",").forEach(p=>{let[k,v]=p.split("=").map(x=>x.trim());if(k)c.skills[k]=v||"normal"});save("character.skills",c.name)}}if(e.target.id==="addLibrary"){let name=prompt("New "+libType.slice(0,-1)+" name");if(name){let x={id:uid(libType.slice(0,-1)),name,category:libType==="armor"?"Armour":libType.slice(0,-1),summary:""};state.library[libType].push(x);selectedLib=x.id;save("library.add",{type:libType,name})}}if(e.target.id==="addCreature"){let name=prompt("Creature name");if(name){let x={id:uid("creature"),name,type:"Monster",size:"Medium",creatureType:"Creature",proficiency:2,ac:10,hp:10,maxHp:10,tempHp:0,speed:30,initiative:0,abilities:{str:10,dex:10,con:10,int:10,wis:10,cha:10},saveProficiencies:[],skills:{},conditions:[],inventory:[],equipment:{},knownActions:[],notes:""};state.creatures.push(x);selectedCreature=x.id;save("creature.add",name)}}if(e.target.id==="addCharacter"){let name=prompt("Character name");if(name){let x={id:uid("pc"),name,type:"PC",class:"Adventurer",level:1,proficiency:2,ac:10,hp:10,maxHp:10,tempHp:0,speed:30,initiative:0,abilities:{str:10,dex:10,con:10,int:10,wis:10,cha:10},saveProficiencies:[],skills:{},conditions:[],inventory:[],equipment:{},knownActions:[],notes:""};state.characters.push(x);selectedChar=x.id;save("character.add",name)}}if(e.target.id==="addNote"){$("#sessionNotes").value+="\n["+new Date().toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"})+"] ";$("#sessionNotes").focus()}if(e.target.id==="exportBtn")exportSession()});document.addEventListener("change",e=>{if(e.target.id==="encounterSelect"){state.activeEncounterId=e.target.value;save()}if(e.target.dataset.edit){let c=entity(e.target.dataset.edit),before=snap(),k=e.target.dataset.key;c[k]=+e.target.value;save("character.update",{id:c.id,field:k},before)}if(e.target.dataset.ability){let c=entity(e.target.dataset.ability),before=snap();c.abilities[e.target.dataset.abilityKey]=+e.target.value;save("character.ability",{id:c.id,ability:e.target.dataset.abilityKey},before)}if(e.target.dataset.equip){let c=entity(e.target.dataset.equip);c.equipment[e.target.dataset.slot]=e.target.value||null;save("equipment.change",{id:c.id,slot:e.target.dataset.slot,item:e.target.value})}if(e.target.dataset.libEdit){let x=state.library[libType].find(y=>y.id===e.target.dataset.libEdit);x[e.target.dataset.field]=e.target.value;save("library.edit",{id:x.id,field:e.target.dataset.field})}});$("#search").addEventListener("input",renderCharacters);document.addEventListener("input",e=>{if(e.target.id==="participantSearch")renderParticipantChoices(e.target.value)});$("#sessionNotes").addEventListener("input",e=>{state.notes=e.target.value;localStorage.setItem("dnd-state",JSON.stringify(state))});$("#sessionName").addEventListener("input",e=>{state.sessionName=e.target.value;$("#sessionTitle").textContent=e.target.value||"Untitled Session";localStorage.setItem("dnd-state",JSON.stringify(state))});function exportSession(){let pack={format:"dnd-session",version:4,exportedAt:new Date().toISOString(),session:{name:state.sessionName,notes:state.notes},changes:state.changes,characters:state.characters,creatures:state.creatures,bystanders:state.bystanders,library:state.library,encounters:state.encounters,activeEncounterId:state.activeEncounterId};let blob=new Blob([JSON.stringify(pack,null,2)],{type:"application/json"}),a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=(state.sessionName||"session").toLowerCase().replace(/[^a-z0-9]+/g,"-")+".dndsession.json";a.click();URL.revokeObjectURL(a.href)}render();if("serviceWorker"in navigator&&location.protocol!=="file:")navigator.serviceWorker.register("sw.js").catch(()=>{});